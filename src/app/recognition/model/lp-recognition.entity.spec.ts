import { LpRecognitionEntity } from './lp-recognition.entity';

describe('LpRecognitionEntity', () => {
  it('should create an instance', () => {
    expect(new LpRecognitionEntity()).toBeTruthy();
  });
});
