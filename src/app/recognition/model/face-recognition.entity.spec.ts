import { FaceRecognitionEntity } from './face-recognition.entity';

describe('FaceRecognitionEntity', () => {
  it('should create an instance', () => {
    expect(new FaceRecognitionEntity()).toBeTruthy();
  });
});
